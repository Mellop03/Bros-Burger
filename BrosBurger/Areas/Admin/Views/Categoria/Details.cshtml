@model BrosBurger.Models.Categoria

@{
    ViewData["Title"] = "Details";
}

<h1 class="H1Título">Detalhes</h1>

<div class="text-light">
    <h4 class="Preto2 w-25">Categoria</h4>
    <hr />
    <dl class="row text-white BackgroundR">
        <dt class = "col-sm-2">
            Nome da categoria
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.NomeCategoria)
        </dd>
        <dt class="co-sm-2">
            Id da categoria
        </dt>
         <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CategoriaID)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.CategoriaID" class="btn btn-primary">Editar</a> |
    <a asp-action="Index" class="btn btn-danger">Voltar</a>
</div>
